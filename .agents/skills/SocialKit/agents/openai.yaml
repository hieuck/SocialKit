interface:
  display_name: "SocialKit"
  short_description: "Repo-specific patterns and workflows for SocialKit"
  default_prompt: "Use the SocialKit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true